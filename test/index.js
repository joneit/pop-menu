'use strict';

/* global describe, it, beforeEach, afterEach */

require('should'); // extends Object with `should`

var FilterTree = require('../src');

describe('`popMenu` that', function() {
    it('is an API', function() {
        FilterTree.should.be.an.Object();
    });
    it('more tests needed');
});
